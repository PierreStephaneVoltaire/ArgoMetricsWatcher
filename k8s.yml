apiVersion: apps/v1
kind: Deployment
metadata:
  name: "argo"
spec:
  selector:
    matchLabels:
      app: "argo"
  template:
    metadata:
      labels:
        app: "argo"
    spec:
      containers:
        - name: "argo"
          image: "gcr.io/argo/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "argo"
spec:
  selector:
    app: "argo"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080